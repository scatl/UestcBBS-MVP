<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled -->
    <item android:color="@android:color/transparent" android:state_enabled="false"/>

    <!-- Selected -->
    <item android:color="?attr/colorPrimaryContainer" android:state_selected="true"/>
    <item android:color="?attr/colorPrimaryContainer" android:state_checked="true"/>

    <!-- Not selected -->
    <item android:alpha="0.7" android:color="?attr/colorOnSurfaceInverse" />
</selector>
